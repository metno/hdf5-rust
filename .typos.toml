[files]
extend-exclude = [
  "hdf5-src/ext",
  "hdf5-sys/src",
]

[default]
extend-ignore-identifiers-re = [
  "Dout",
]
# Intentional historical HDF5 misspellings, normalized in error.rs scrub()
extend-ignore-re = [
  "accessability",
  "accessibilty",
]
